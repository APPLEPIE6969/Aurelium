package com.aureleconomy.database.types;

/**
 * SQLite dialect. {@link SQLTypes} already spells every type the SQLite way, so there is
 * nothing to override.
 */
public class SQLiteTypes extends SQLTypes {
}
